import { corpus } from "./Corpus";
// Use named export:
// export const buildDictionary = () => {
//   console.log("cats");
// };
// or if we only are going to have one function in here:
export default function(corpus) {
  console.log(corpus);

  /* Go through document collection (corpus) and tokenize each document */
  
  
}
